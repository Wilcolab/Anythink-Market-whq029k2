/**
 * Write a function called `toCamelCase` that takes a string as input and returns the string converted to camelCase.
 * The function should remove spaces, dashes, and underscores, and capitalize each word except the first one.
 * 
 * Example:
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("convert_this-string") // returns "convertThisString"
 */